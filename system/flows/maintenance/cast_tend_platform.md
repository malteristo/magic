# Instruments — turtleOS body

**Purpose:** What obligation 2 can open. Not a checklist. Skip what is green.
**Invocation:** opened from `cast_maintenance_arrival.md` · `@tend-platform` still lands there.

Spirit fixes plumbing. Mage dogfoods UX when a capability chapter closes (`functional-gate-protocol.md`). Relic `@turtle-care` is the glance below — not a wellbeing ritual.

---

## Live instance

**SHA pair (F-84).** `./scripts/sync_practice_root.sh pull` first, then `desk/craft/automation-reports/latest.md` — the file on disk after pull. If Mini `~/turtleos` SHA ≠ report SHA, that is the finding. Do not narrate the report's dirty-tree JSON as current body.

**Checkout.** SSH: `~/turtleos` status, SHA vs `origin/main`. Clean tree before pull. `./venv/bin/python3` for tests. Pull without restart when only docs/scripts changed; restart **both** services when runtime Python changed:

```bash
launchctl kickstart -k gui/$(id -u)/com.turtle.discord
launchctl kickstart -k gui/$(id -u)/com.turtle.river
```

Process start times must be after the pull (`docs/learnings.md` deploy pitfall).

**Gate.** Pass and no deploy since report → trust it. FAIL or deploy since → Mini `unittest discover -s tests -q` and `shake_report.py --write --strict`. Live shakes only for the **changed surface**.

**Last-ship claims (F-83).** When the last turtleOS chapter is ≤2 days — including `. maintenance turtleOS`. New scenario ids, `SHAKE_ARTIFACTS` maps, matrix Actions: the proving modules must run, not only be named. A map stamped onto a prior pass is the silent-lie class.

**Quality row.** `python3 scripts/quality_baseline.py` — one line if unused runtime or modules ≥1000 moved; skip if the last row is unchanged.

**Fossils — when a signal appears** (or once on a deep tend): a port/Caddy route to a retired service; `.env` keys the spec marks retired; live `launchctl` rows naming `bridge` / `livesync` / `couch`; canaries that would go green for the wrong reason. Confirm retirement in law, kill exposure, then config, then code. Leave large binaries for a second proposal. Do not resurrect a retired path to verify it. Occasion: 2026-08-02, Caddy `:5985` after CouchDB was already down.

**Glance (live only).** Recent `story/eddies/` on active roots; latest Turtle proposal if newer than last tend. Surface: heat vs silence, hard terrain (shape only), living verify that needs his eye. Not a scheduled acknowledgment to Turtle. Skip in one line when quiet.

**When a signal:** Discord health, thread-eddy dissolution, Turtle proposal triage, magic↔github divergence (separate merge chapter).

---

## Published product

CI and the nightly gate *are* this habitat. Offline shakes, unit suite, `quality_baseline`, matrix, doc topology. No SSH, no `desk/`, no names. A product defect found live is committed here; an instance fact is not.

---

## Close

Ops verdict, SHA pair, services, last-ship result, glance or "quiet." Comprehensive continues to the workshop catalog. No care-spell handoff.

## Related

- `turtleos/docs/automation/registry.md` · `docs/automation/functional-gate-protocol.md`
- `cast_shake.md` · `cast_calibrate.md`
- Retired: `cast_turtle_care.retired.md`
