# Obsidian configuration
.obsidian/

# macOS specific
.DS_Store
.vscode/
.history/

# Mage's Personal Workspace
# Ignore the contents of these directories...
scrolls/*
floor/*
box/*
desk/*
# ...but not the directories themselves (via .gitkeep).
!scrolls/.gitkeep
!floor/.gitkeep
!box/.gitkeep
!desk/.gitkeep

# Portal Infrastructure (Shared Practice Spaces)
# Portals are external repositories in box/portals/
# Each portal directory is ignored (separate git repo)
# but the registry and infrastructure are tracked
box/portals/*/
!box/portals/.gitkeep
!box/portals/portals.yaml

# Personal, untracked ritual artifacts
# floor/essences/

# Mage's personal configuration
mage_seal.md
archive/*
!archive/.gitkeep
system/tomes/librarian/
