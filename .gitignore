# Obsidian configuration
.obsidian/

# macOS specific
.DS_Store
.vscode/
.history/

# Mage's Personal Workspace
# Ignore the contents of these directories...
scrolls/*
floor/*
box/*
desk/*
# ...but not the directories themselves (via .gitkeep) and README files.
!scrolls/.gitkeep
!floor/.gitkeep
!floor/README.md
!floor/spirit_lineage.md
!box/.gitkeep
!box/README.md
!desk/.gitkeep
!desk/README.md

# Portal Infrastructure (Shared Practice Spaces)
# Portals are external repositories in portals/
# Each portal directory is ignored (separate git repo)
# but the infrastructure files are tracked
portals/*/
!portals/upstream/
portals/upstream/*/
!portals/registry.yaml
!portals/README.md

# Circle Infrastructure (My Offerings)
# Circles are external repositories in circles/
# Each circle directory is ignored (separate git repo)
# but the infrastructure files are tracked
circles/*/
!circles/registry.yaml
!circles/README.md

# Personal, untracked ritual artifacts
# floor/essences/

# Mage's personal configuration
mage_seal.md
AGENTS.md
# Archive directories (anywhere in tree)
**/archive/
system/tomes/librarian/
